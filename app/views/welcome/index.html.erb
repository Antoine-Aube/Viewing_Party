<!--<div class="text-center">
  <h1 class="text-5xl text-green-500 font-bold text-teal-500">Current Users</h1>
    <% @users.each do |user| %>
      <ul class= "text-indigo-500">
        <li><%= link_to user.name, "/users/#{user.id}" %></li>
      </ul>
    <%end %>
</div>-->

<div class="text-center text-2xl text-green-500 font-bold bg-gray-900">
  <p>Welcome to your one stop shop for watching movies with your friends</p>
  <br>
  <p> Log in or create an account to create events and discover movies</p>

<div class="flex flex-col items-center text-center mt-6 bg-gray-900">
  <% @top_movies.each do |movie| %>
    <div class="mb-4">
     <p class="text-xl text-green-500 font-semibold"><%= movie.title %></p>
      <%= image_tag("https://image.tmdb.org/t/p/w500#{movie.backdrop_path}", class: "w-full max-w-sm rounded shadow-md") %>
    </div>
  <% end %>
  </div>
</div>